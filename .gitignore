# lockfiles we don't use & generated files
package-lock.json
public/sitemap-*.xml
*.bak
*.backup-*
*.broken-*

# optional helper scripts (uncomment if you don't want them in git)
# *.cmd
node_modules/
.next/
.vercel/
public/sitemap-*.xml
app/*.bak*
*.cmd
